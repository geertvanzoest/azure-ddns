---
phase: 03-rename-naar-ddns4j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ddns4j
  - test/test_helper.bash
  - test/test_locking.bats
  - test/test_ttl.bats
  - test/test_ip_validation.bats
  - test/test_ip_fallback.bats
  - test/test_verbose.bats
  - test/test_force_flag.bats
autonomous: true
requirements: [REN-01, REN-02, REN-03]

must_haves:
  truths:
    - "Het script heet ddns4j en is uitvoerbaar (chmod +x)"
    - "Het oude bestand ns4j bestaat niet meer"
    - "Lock file pad is /tmp/ddns4j.lock"
    - "Alle bats tests slagen met de nieuwe naam ddns4j"
    - "Nergens in de codebase (buiten .planning/ en .claude/) komt ns4j voor"
  artifacts:
    - path: "ddns4j"
      provides: "Hernoemd script met ddns4j lock file"
      contains: "/tmp/ddns4j.lock"
    - path: "test/test_helper.bash"
      provides: "Test helper die ddns4j laadt"
      contains: "DDNS4J_SCRIPT"
    - path: "test/test_locking.bats"
      provides: "Locking tests met ddns4j.lock"
      contains: "/tmp/ddns4j.lock"
  key_links:
    - from: "test/test_helper.bash"
      to: "ddns4j"
      via: "DDNS4J_SCRIPT pad referentie"
      pattern: "DDNS4J_SCRIPT.*ddns4j"
    - from: "test/test_locking.bats"
      to: "ddns4j"
      via: "LOCK_FILE assertion"
      pattern: "/tmp/ddns4j\\.lock"
    - from: "test/*.bats"
      to: "test/test_helper.bash"
      via: "load_ddns4j functie-aanroep"
      pattern: "load_ddns4j"
---

<objective>
Hernaam het volledige project van ns4j naar ddns4j: script-bestand, interne referenties (lock file), en de complete test suite.

Purpose: De projectnaam ddns4j consistent doorvoeren zodat alle bestanden, variabelen en tests de juiste naam gebruiken.
Output: Hernoemd script `ddns4j`, bijgewerkte test suite die volledig groen draait.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ns4j
@test/test_helper.bash
@test/test_locking.bats
@test/test_ttl.bats
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hernaam script en interne referenties (REN-01, REN-02)</name>
  <read_first>
    - ns4j (het huidige script, 267 regels)
  </read_first>
  <files>ddns4j</files>
  <action>
1. Kopieer `ns4j` naar `ddns4j` met `git mv ns4j ddns4j`
2. In `ddns4j` regel 13: wijzig `readonly LOCK_FILE="/tmp/ns4j.lock"` naar `readonly LOCK_FILE="/tmp/ddns4j.lock"`
   Dit is de ENIGE wijziging in het script zelf -- alle andere code blijft identiek.
3. Controleer dat `ddns4j` uitvoerbaar is (`chmod +x ddns4j` als nodig na git mv)
4. Verifieer dat `ns4j` niet meer bestaat als los bestand

Geen andere regels in het script bevatten de string "ns4j" -- alleen LOCK_FILE op regel 13.
  </action>
  <verify>
    <automated>test -x ddns4j && ! test -e ns4j && grep -q '/tmp/ddns4j.lock' ddns4j && ! grep -q 'ns4j' ddns4j</automated>
  </verify>
  <acceptance_criteria>
    - Bestand `ddns4j` bestaat en is uitvoerbaar (executable bit set)
    - Bestand `ns4j` bestaat NIET meer
    - `ddns4j` regel 13 bevat exact: `readonly LOCK_FILE="/tmp/ddns4j.lock"`
    - De string "ns4j" komt nergens voor in `ddns4j` (grep -c "ns4j" ddns4j geeft 0)
  </acceptance_criteria>
  <done>Script heet ddns4j, is uitvoerbaar, lock file verwijst naar /tmp/ddns4j.lock, oud bestand ns4j verwijderd</done>
</task>

<task type="auto">
  <name>Task 2: Hernaam test suite referenties (REN-03)</name>
  <read_first>
    - test/test_helper.bash (volledige inhoud, 105 regels)
    - test/test_locking.bats (volledige inhoud, 41 regels)
    - test/test_ttl.bats (volledige inhoud, 28 regels)
    - test/test_ip_validation.bats (alleen regel 8: load_ns4j)
    - test/test_ip_fallback.bats (alleen regel 8: load_ns4j)
    - test/test_verbose.bats (alleen regel 8: load_ns4j)
    - test/test_force_flag.bats (alleen regel 8: load_ns4j)
  </read_first>
  <files>
    test/test_helper.bash
    test/test_locking.bats
    test/test_ttl.bats
    test/test_ip_validation.bats
    test/test_ip_fallback.bats
    test/test_verbose.bats
    test/test_force_flag.bats
  </files>
  <action>
**test/test_helper.bash** -- 6 wijzigingen:
1. Regel 2: commentaar "ns4j bats tests" -> "ddns4j bats tests"
2. Regel 3: commentaar "ns4j functies" -> "ddns4j functies"
3. Regel 5: commentaar "ns4j script" -> "ddns4j script"
4. Regel 6: `NS4J_SCRIPT="${BATS_TEST_DIRNAME}/../ns4j"` -> `DDNS4J_SCRIPT="${BATS_TEST_DIRNAME}/../ddns4j"`
5. Regel 9: `load_ns4j()` -> `load_ddns4j()`
6. Regel 19: `source "$NS4J_SCRIPT"` -> `source "$DDNS4J_SCRIPT"`
7. Regel 23: commentaar "ns4j gebruikt" -> "ddns4j gebruikt"

**test/test_locking.bats** -- 4 wijzigingen:
1. Regel 14: test naam `"OPS-03: LOCK_FILE is /tmp/ns4j.lock"` -> `"OPS-03: LOCK_FILE is /tmp/ddns4j.lock"`
2. Regel 16: assertion `[ "$LOCK_FILE" = "/tmp/ns4j.lock" ]` -> `[ "$LOCK_FILE" = "/tmp/ddns4j.lock" ]`
3. Regel 20: `grep -q "flock --nonblock" "$NS4J_SCRIPT"` -> `grep -q "flock --nonblock" "$DDNS4J_SCRIPT"`
4. Regel 25: `exec 200>/tmp/ns4j.lock` -> `exec 200>/tmp/ddns4j.lock`
5. Regel 33: `run bash "$NS4J_SCRIPT"` -> `run bash "$DDNS4J_SCRIPT"`

**test/test_ttl.bats** -- 2 wijzigingen:
1. Regel 22: test naam `"DNS_TTL wordt gebruikt in ns4j script"` -> `"DNS_TTL wordt gebruikt in ddns4j script"`
2. Regel 23: `grep -q 'DNS_TTL:-300' "$NS4J_SCRIPT"` -> `grep -q 'DNS_TTL:-300' "$DDNS4J_SCRIPT"`
3. Regel 27: `grep -q 'DNS_TTL' "$NS4J_SCRIPT"` -> `grep -q 'DNS_TTL' "$DDNS4J_SCRIPT"`

**test/test_ip_validation.bats, test/test_ip_fallback.bats, test/test_verbose.bats, test/test_force_flag.bats** -- elk 1 wijziging:
- `load_ns4j` -> `load_ddns4j` (op regel 8 in elk bestand)
  </action>
  <verify>
    <automated>cd /Users/geert/src/hscjeka/ns4j && ! grep -r 'ns4j' test/ && bats test/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "ns4j" test/` geeft GEEN resultaten (exit code 1)
    - `grep -r "ddns4j" test/test_helper.bash` matcht DDNS4J_SCRIPT, load_ddns4j, en ddns4j commentaren
    - `grep "load_ddns4j" test/test_*.bats` matcht in alle 5 test-bestanden (ip_validation, ip_fallback, verbose, force_flag, locking)
    - `bats test/` draait alle tests en geeft exit code 0
    - test/test_locking.bats bevat exact de string `/tmp/ddns4j.lock` (3x)
    - test/test_ttl.bats bevat "ddns4j script" in de test naam
  </acceptance_criteria>
  <done>Alle test-bestanden refereren aan ddns4j, load_ddns4j, DDNS4J_SCRIPT -- volledige test suite is groen</done>
</task>

</tasks>

<verification>
Na beide taken:
1. `test -x ddns4j && ! test -e ns4j` -- script hernoemd en uitvoerbaar
2. `grep -q '/tmp/ddns4j.lock' ddns4j` -- lock file pad correct
3. `! grep -r 'ns4j' ddns4j test/` -- geen restanten van oude naam (buiten .planning/ en .claude/)
4. `bats test/` -- volledige test suite groen
</verification>

<success_criteria>
- Het script-bestand heet `ddns4j` en is uitvoerbaar
- Lock file pad is `/tmp/ddns4j.lock`
- Alle 7 test-bestanden refereren aan `ddns4j` / `DDNS4J_SCRIPT` / `load_ddns4j`
- `bats test/` slaagt met 0 fouten
- De string "ns4j" komt nergens meer voor buiten .planning/ en .claude/ directories
</success_criteria>

<output>
Na voltooiing, maak `.planning/phases/03-rename-naar-ddns4j/03-01-SUMMARY.md`
</output>
